{
  "name": "vitany-feed",
  "version": "1.0.0",
  "private": true,
  "description": "VITANY icerik plani - Instagram feed onizleme (statik + build-time manifest)",
  "scripts": {
    "build": "node scripts/generate-manifest.js"
  }
}
